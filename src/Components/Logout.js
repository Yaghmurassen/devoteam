import React, { useState, useEffect, Fragment } from "react";
import { fetchProfiles } from "../Data/queries";

import "../Assets/scss/main.scss";

const Logout = () => {
  return <article className="logout"></article>;
};

export default Logout;
